[settings]
lockfile = true

[tools]
uv = "0.12"

[tasks.install]
description = "Install dependencies"
run = "uv sync"

[tasks.build]
description = "Build the source and wheel distributions"
run = "uv build"

[tasks.run]
description = "Run the xasm CLI"
run = "uv run --with-editable . xasm"
